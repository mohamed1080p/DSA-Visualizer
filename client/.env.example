# Optional. Leave empty in development to use Vite proxy (/api -> http://127.0.0.1:5258).
# Start the API first: dotnet run --project DSA-Visualizer --launch-profile http
# For production static hosting, set to your API origin (no trailing slash), e.g.:
# VITE_API_BASE_URL=https://api.yourdomain.com
VITE_API_BASE_URL=
